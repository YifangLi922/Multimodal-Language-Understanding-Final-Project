# Relsim Project Timeline

## 先想清楚：你接下来要搭的到底是什么

抛开所有细节，你的整个实验其实就是一句话循环：

> 给一堆图，让每个模型打分，看谁的打分更符合关系逻辑，再按关系类型分开看谁强谁弱。

拆成四个零件，你要搭的就是这四个：

1. **一批分好类的评估数据**（哪些图属于时间演变、哪些属于嵌套、哪些属于容器）——这是原料。
2. **三个打分器**（relsim、CLIP、DINO），每个都能对图输出一个相似度分数——这是工具。
3. **一套评估逻辑**（把打分变成“准确率”这个数字）——这是尺子。
4. **按关系类型拆开统计**（三类分别算，看落差）——这是你的发现所在。

你在德国要做的，就是**把这四个零件都做出来，串成一条能一键跑完的流水线**。回家后，数据、代码、结果都在手上了，写作就只是“把已经发生的事情讲清楚”。

一个关键的策略，我必须先跟你强调，它能救你：

> **先用最小规模把整条链路从头到尾跑通一次，再扩大规模。**

不要先花三天精心构造 100 个三元组，再去跑模型——万一模型跑不通，你前三天全白费。正确顺序是反过来的：**先拿三五个样本，把“数据 → 打分 → 出准确率”全流程跑通（哪怕结果很粗糙），确认管道不漏水，再往里灌更多数据。**这叫“打通垂直切片”，是项目经验里最重要的一条保命原则。

## 从今天开始的行动清单（按先后顺序）

我把它分成五个阶段，前面是“必须先做、卡住了最麻烦的”，后面是“确定能做、只是耗时间”的。**严格按顺序来，不要跳。**

### 阶段 A：把环境和模型跑通（最优先，今明两天）

这是最容易卡人、最该趁“还在学校、能找人问、能连校园 GPU”时解决的部分。

1. 在学校 GPU 上，按作者 GitHub 的说明 `pip install relsim`，把依赖装好。
2. 找到作者仓库里那个“给两张图打分”的最小示例（README 里说三行代码就能出分），**随便拿两张图跑通它**，确认你能拿到一个 relsim 分数。
3. 同样地，把 **CLIP 和 DINO** 各自“给图算 embedding、再算两图余弦相似度”跑通——各拿两张图，确认能出分数。

**这一阶段的唯一目标：三个模型，每个都能对“任意两张图”吐出一个数。**做到这个，最大的技术风险就过去了。**如果这里卡住（依赖冲突、显存不够、GPU 排队），今天就要暴露出来，趁还在德国赶紧解决/求助。**

### 阶段 B：串出“评估一个三元组”的最小流程（第 2 天）

先不管数据多不多，手动准备**一个三元组**（3 张图：锚点、关系相似的正样本、外观相似的负样本），随便从作者数据里挑三张凑出来，对错无所谓，先跑通流程：

1. 写一小段代码：输入三张图 → 让 relsim 分别算 `sim(锚, 正)` 和 `sim(锚, 负)` → 判断 `sim(锚, 正) > sim(锚, 负)` 是否成立（成立记 1，不成立记 0）。
2. 让 CLIP、DINO 也做同样的事。
3. **此刻你就有了一条完整的垂直切片：一个三元组进去，三个模型各吐一个 0/1 出来。整条流水线跑通了。**

做到这里，你其实已经把整个项目最难的部分做完了。后面全是“往这个管道里灌更多数据”和“把结果分组统计”，都是确定能做的活。

### 阶段 C：批量构造评估数据（第 3–4 天，耗时间但不难）

现在才开始正经准备数据。先做 **Step 0 的数据关系地图**（它同时帮你确认三类关系样本够不够）：

1. 下载作者的 `anonymous-captions-114k`，用 pandas 读进来（前面说过，文本才 19MB，很小）。
2. 随机抽 300–500 条 caption，人工归类到你的三类关系（时间演变 / 嵌套 / 容器）+ 其它，统计分布。**特别留意容器类够不够**——上一轮我们担心它可能被作者筛掉了，这一步就是验证的时候。如果不够，就换一类或从 14k 随机图里补。
3. 确定三类关系后，**半自动构造三元组**：用 caption 文本相似度找关系相似的正样本，用 CLIP 视觉相似度找外观相似的负样本，你在候选里人工点选。每类先做 25–30 个，外加纯外观组 20–25 个。

> **关键提醒：**构造数据时先做一类关系就好（比如时间演变），做够 25 个，立刻进入阶段 D 跑一遍。**别三类一次性全造完再跑**——先跑通一类，确认结果合理，再补另外两类。

### 阶段 D：跑完整实验、拿到结果（第 4–5 天）

1. 把你造好的三元组喂进阶段 B 搭好的流水线，批量跑。
2. **按关系类型分组**，分别算每个模型在每类上的三元组准确率。
3. 得到你的核心结果表：行是关系类型，列是模型，格子里是准确率——**relsim 和 CLIP/DINO 的落差随关系类型的变化，就是你的发现。**
4. 顺手把每个模型判错的几个案例存下来（截图/记录），回家写“定性分析”时要用。

### 阶段 E：存好一切，为回家写作做准备（临走前）

1. 把所有结果（准确率表、原始分数、失败案例）**导出成文件存好**（CSV、截图都行），确保**离开 GPU 也能访问**——回家你多半连不上校园 GPU 了，结果必须落地保存。
2. 代码推到 GitHub，写个简单 README。
3. 如果时间紧，**两类关系 + 纯外观组的结果就够写一份完整 report 了**，第三类有余力再补。

## 一句话记住整个节奏

> **先让三个模型能打分（A）→ 用一个三元组把流水线跑通（B）→ 造一类关系的数据先跑一遍（C→D）→ 确认没问题再扩到三类 → 结果全部存盘带回家（E）。**

核心心法就一条：**永远先跑通一个最小的完整版本，再扩大规模。**每一步都让你手里有一个“虽然小但完整”的东西，这样无论哪天时间被打断，你都不会落到“什么都没跑通”的境地。
